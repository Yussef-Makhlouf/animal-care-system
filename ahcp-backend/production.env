# AHCP Backend Production Environment Variables
NODE_ENV=production
PORT=3001

# Database
# ⚠️ IMPORTANT: Change this to your production MongoDB connection string
MONGODB_URI=mongodb+srv://yussefmakhloufiti_db_user:Yussef12345@cluster0.pgy8qei.mongodb.net/ahcp_database?retryWrites=true&w=majority&appName=Cluster0


# JWT Configuration
# ⚠️ CRITICAL: Generate a strong random secret key before deployment!
# You can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=ahcp_super_secret_key_2024_development_only_change_in_production_123456789
JWT_EXPIRES_IN=7d

# API Configuration
# ⚠️ Set to false in production or protect with authentication
API_DOCS_ENABLED=false
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration
# ⚠️ IMPORTANT: Specify your frontend domain instead of *
# Example: CORS_ORIGIN=https://your-domain.com
CORS_ORIGIN=http://localhost:3000

# Email Configuration (Optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@ahcp.gov.sa

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_PATH=uploads

# Bcrypt Rounds
BCRYPT_ROUNDS=12
